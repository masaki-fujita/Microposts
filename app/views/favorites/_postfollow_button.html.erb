
    <% if current_user.postfollowing?(micropost) %>
        <%= form_with(model: current_user.favorites.find_by(micropost_id: micropost.id), local: true, method: :delete) do |f| %>
            <%= hidden_field_tag :user_id, micropost.id %>
            <%= f.submit "お気に入りから外す", class: "btn btn-danger " %>
        <% end %>
    <% else %>
        <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
            <%= hidden_field_tag :user_id, micropost.id %>
            <%= f.submit "お気に入りに追加", class: "btn btn-primary " %>
        <% end %>
    <% end %>

